/** Tier checks, paywall entry points (Phase 9+). */

/**
 * Whether the user has Pro. Phase 8 stub: always false until RevenueCat / paywall lands.
 */
export function isProUser(): boolean {
  return false;
}
